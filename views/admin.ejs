<%- include('templates/header.ejs') %>
    <div>Users:
        <% if (isAdmin) { %>
            <h2> Only admins can view this. </h2>
            <% } %>
                <ul>
                    <% todos.forEach(function(todoItem){ %>
                        <li>
                            <ul>
                                <li>
                                    <%= todoItem.name %>
                                </li>
                                <li>
                                    <%= todoItem.done %>
                                </li>
                        </li>
                </ul>
                </li>
                <% }); %>
                    </ul>

                    <form action="/addNewToDoItem" method="POST">
                        <label for="x">Add next item</label>
                        <input id="x" type="text" name="x">
                        <input type="submit" value="Add">
                    </form>
    </div>
    <%- include('templates/footer.ejs') %>